<nav class="navbar navbar-expand-lg navbar-light bg-light">

    <div>
        <%= image_tag "logo.ico",:alt => "Logo", class: "rounded-circle" %>
    </div>

    <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
        <p class="nombre-nav"> CODISAN </p>
    </div>

   <div class="btn-group">
        <button type="button" class="btn btn-secondary dropdown-toggle" data-bs-toggle="dropdown" data-bs-display="static" aria-expanded="false">
             Menu
        </button>
        
        <ul class="dropdown-menu dropdown-menu-lg-end">
            <li class= "img_avatar">
                <%if current_user.avatar? %>
                <% end%>
                <%= image_tag(current_user.avatar.url, :size => "40x40", class: "user_img") %> 
                

            </li>
            <li><%= button_to "Inicio", root_path, method: :get, class: "dropdown-item" %> </li>
            <li><%= button_to "Perfil", current_user, method: :get, class: "dropdown-item" %></li>
            <% if current_user.has_role? :admin %>
                <li><%= button_to "Admin",  users_path, method: :get,  class: "dropdown-item" %></li>
            <% end %>
            <li><%= button_to "Cerrar", destroy_user_session_path, method: :get, class: "dropdown-item" %></li>
            
        </ul>
    </div>
</nav>

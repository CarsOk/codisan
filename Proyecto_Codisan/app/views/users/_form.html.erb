<div class="form-editar"> 
    <%= form_with(model: @user) do |form| %>
    <%= form.collection_check_boxes :role_ids, Role.all, :id, :name, class:"form-check-input", id:"inlineCheckbox1," %>
    <br>
    <%= form.submit "Guardar", onclick: "return confirmDestroy('¿Desea guardar esto?')" %>
    <% end %>
</div>


<script>
function confirmDestroy(message) {
  if (!confirm(message)) {
    return false;
  }
}
</script>



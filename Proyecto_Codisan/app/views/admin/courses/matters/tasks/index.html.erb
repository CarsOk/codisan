<%= button_to 'crear actividad',  new_admin_course_matter_task_url(@matter), method: :get%>
<% @tasks.each do |tarea| %> 
    <%= tarea.name_activity %>
    <%= tarea.description_activity %>
    <%= tarea.created_at %>

<%end%>

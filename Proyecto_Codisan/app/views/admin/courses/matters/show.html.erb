<%= render 'layouts/navbar' %>
<h2><%=  @matter.name_matter %>
<%=  @course.name_course %></h2>
  <table class="table">
    <thead class="thead-dark">
      <tr>
        <th scope="col">N° de documento</th>
        <th scope="col">Nombres</th>
        <th scope="col">Apellidos</th>
      </tr>
    </thead>
       <%= render "form_user"%>
  </table>
  <%= button_to "Editar lista estudiantes", admin_course_users_url(@course, @user), method: :get%> 
<%= render 'layouts/footer' %>
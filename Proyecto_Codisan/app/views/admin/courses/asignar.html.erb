Vista con formulario de asignacion

<%= simple_form_for([:admin, @course], 
    :url => url_for(:action => 'guardar'),
    :method => 'post') do |f| %>

    
    <%= f.association :matters,  as: :check_boxes, label_method: :name_matter, value_method: :id, include_hidden: false %>
    <br>

    <%= submit_tag 'Guardar' %>

<% end%>